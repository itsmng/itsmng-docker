apiVersion: v1
kind: Service
metadata:
  name: itsm-ng
spec:
  selector:
    app.kubernetes.io/name: itsm-ng
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
